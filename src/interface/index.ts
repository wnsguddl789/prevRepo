export * from './socket';

export type chatType = {
  name: string;
  message: string;
};

export type keyWordType = {
  index: string;
  keyword: string;
};
export type userType = {
  userId: string;
  name: string;
};
